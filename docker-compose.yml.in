version: '2'

services:
  web:
    # Existing image in repository
    # image: kthse/docker pull kthse/kth-style-web:13.37

    # Build from Dockerfile in current directory
    build: .

    # Port published from inside the Docker image
    ports:
      - 3000
